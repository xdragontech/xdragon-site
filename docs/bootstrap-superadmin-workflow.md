**Status**
Retired from `xdragon-site`.

Bootstrap superadmin ensure/recover workflows now belong to `command`. Do not run these commands from `xdragon-site`.

**Use Instead**
- run the equivalent `bootstrap-superadmin:*` commands from the `command` repo
- use the Command admin/config diagnostics for live bootstrap-account checks
