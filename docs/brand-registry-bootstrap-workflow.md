**Status**
Retired from `xdragon-site`.

Brand registry ownership now lives in `command`. Do not run brand-registry bootstrap or sync commands from this repo.

**Use Instead**
- run the equivalent `brand:*` commands from the `command` repo
- manage brands and hosts through the Command backoffice, not through `xdragon-site`
