**Status**
Retired from `xdragon-site`.

Brand email bootstrap ownership now lives in `command`. Do not run brand email sync commands from this repo.

**Use Instead**
- run the equivalent `brand-email:*` commands from the `command` repo
- manage live brand email config through the Command backoffice
